# Product of Array Except Self

## https://leetcode.com/problems/product-of-array-except-self


## Difficulty
Easy / Medium / Hard

## Pattern
(Prefix + Suffix)

---

## Brute Force Approach

### Idea
Instead of using division:

Compute prefix (left) products.

Compute suffix (right) products.

Multiply them togethe

### Time Complexity
O(n)

### Space Complexity
O(1)

---

## Optimized Approach

### Idea
Explain briefly

# C sharp code
``` csharp
public class Solution {
    public int[] ProductExceptSelf(int[] nums) {
        int[] leftP = new int[nums.Length];
        int[] rightP = new int[nums.Length];
        int[] p = new int[nums.Length];
        leftP[0] = 1;
        for(int i = 0; i < nums.Length-1; i++)
        {
            leftP[i+1] = leftP[i] * nums[i];
        }

        rightP[nums.Length-1] = 1;
        for(int i = nums.Length-1; i > 0; i--)
        {
            rightP[i-1] = rightP[i] * nums[i];
        }
        
        for(int j= 0; j< nums.Length; j++)
        {
            p[j] =  rightP[j] * leftP[j];

        }
        return p;
    }
}
